@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <table id="employeesTable" class="table table-bordered table-secondary">
        <thead>
            <tr>
                <th>姓名</th>
                <th>年資</th>
                <th>薪水</th>
            </tr>
        </thead>
        <tbody>
        </tbody>
    </table>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>

@section Scripts{
<script>
        //JSON
        // [index:value, index:value, index:value] {"key":value, "key":value}
        // [{"key":value, "key":value}, {"key":value, "key":value}]
        // {"key":[value, value, value]}

        // console.log(emps[2].salary);
        //for(let i=0,max=emps.length;i<max;i++){
        //     console.log(emps[i].name)
        //     console.log(emps[i].workYears)
        //     console.log(emps[i].salary)
        //}
        const emp = {
            "employees": [
                { "name": "Tom", "workYears": 3, "salary": 35000 },
                { "name": "Jack", "workYears": 5, "salary": 40000 },
                { "name": "Mary", "workYears": 7, "salary": 45000 }
            ]
        };
        //解構賦值
        //for (let i = 0, max = emp.employees.length; i < max; i++) {
        //    const { name, workYears, salary } = emp.employees[i];
        //    console.log(name);
        //    console.log(workYears);
        //    console.log(salary);
        //}

        //新寫法
        //emp.employees.forEach(theEmp => 
        //{
        //const { name, workYears, salary } = theEmp;
        //console.log(name);
        //console.log(workYears);
        //console.log(salary);
        //})

        //let datas = ""
        //emp.employees.forEach(theEmp => {
        //    const { name, workYears, salary } = theEmp;
        //    const data = `
        //       <tr>
        //         <td>${name}</td>
        //         <td>${workYears}</td>
        //         <td>${salary}</td>
        //       </tr>
        //       `;
        //  datas += data;
        //})
        //document.querySelector('#employeesTable>tbody').innerHTML = datas;

        //const datas = emp.employees.map(theEmp => {
        //    const { name, workYears, salary } = theEmp;
        //    return (
        //        `<tr>
        //                  <td>${name}</td>
        //                  <td>${workYears}</td>
        //                  <td>${salary}</td>
        //                  </tr>
        //                `
        //    )
        //})
        //document.querySelector('#employeesTable>tbody').innerHTML = datas.join("");

        //map 回傳的是陣列值 用 join 把每筆陣列值讀出來

        //用Jquery $.each的應用
        let datas = "";
        $.each(emp.employees, function (index, theEmp) {
            const { name, workYears, salary } = theEmp;
            const data = `
               <tr>
                 <td>${name}</td>
                 <td>${workYears}</td>
                 <td>${salary}</td>
               </tr>
               `;
            datas += data;
        });
        $('#employeesTable>tbody').html(datas);
</script>
}
@section Styles{
    <style>
        h1{
            background-color:yellow;
        }
    </style>
}
